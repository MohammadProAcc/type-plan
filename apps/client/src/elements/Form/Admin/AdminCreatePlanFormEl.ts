import styled from "styled-components";
import { Padding } from "styles";
import { Margin } from "styles/design/Margin";
import { Form } from "..";

export const AdminCreatePlanFormEl = styled(Form)`
  padding: ${Padding.l3} 0;
  margin-top: ${Margin.l5};
`
