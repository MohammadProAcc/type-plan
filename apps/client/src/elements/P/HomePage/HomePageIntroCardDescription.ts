import styled from 'styled-components';
import { FontFamily } from 'styles';
import { P } from '..';

export const HomePageIntroCardDescription = styled(P)`
  padding-top: 0.125rem;
  font-family: ${FontFamily.medium};
`;
