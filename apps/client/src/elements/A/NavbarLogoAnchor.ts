import styled from 'styled-components';
import { A } from '.';

export const NavbarLogoAnchor = styled(A).attrs({
  href: '/',
  title: 'نقشه',
})`
  width: 3rem;
  height: 3rem;
  margin-left: 2rem;
`;
