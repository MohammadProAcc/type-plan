import styled from 'styled-components';
import { Padding } from 'styles';
import { Div } from 'elements/Div/Div';

export const BlogPageLastPostDate = styled(Div)`
  padding-right: ${Padding.l3};
  padding-left: ${Padding.l7};
`;
