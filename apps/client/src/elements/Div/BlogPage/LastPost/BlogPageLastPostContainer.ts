import styled from 'styled-components';
import { Color, Padding, Margin, Radius } from 'styles';
import { Div } from '../../Div';

export const BlogPageLastPostContainer = styled(Div)`
  display: flex;
  flex-direction: column;
`;
