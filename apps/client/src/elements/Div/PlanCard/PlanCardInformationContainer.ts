import styled from 'styled-components';
import { Div } from '../Div';

export const PlanCardInformationContainer = styled(Div)`
  width: 100%;

  display: flex;
  flex-direction: column;
  justify-content: space-around;
  row-gap: 0.25rem;
`;