import styled from 'styled-components';
import { Div } from '../Div';
import { Color } from 'styles';

export const PlanInformationContainerTitle = styled(Div)`
  width: 100%;
  padding: 0.5rem;
  text-align: center;
  background-color: ${Color.BackgroundSecondary};
`;
