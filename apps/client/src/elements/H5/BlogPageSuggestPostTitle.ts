import styled from 'styled-components';
import { Margin, Padding } from 'styles';
import { H5 } from './H5';

export const BlogPageSuggestPostTitle = styled(H5)`
  padding-right: ${Padding.l2};
  margin-top: ${Margin.l5};
  margin-bottom: 0;
`;
