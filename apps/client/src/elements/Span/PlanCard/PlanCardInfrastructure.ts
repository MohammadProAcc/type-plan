import styled from "styled-components";
import { FontFamily, FontSize } from "styles";
import { Em } from "../Em";

export const PlanCardSeparator = styled(Em)`
  font-family: ${FontFamily.medium};
  font-size: ${FontSize.lg};
`;
