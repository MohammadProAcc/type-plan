import { H1 } from "./H1";
import styled from 'styled-components'
import { Padding } from "styles";
export const AdminPageTitle = styled(H1)`
  width: auto;
  padding: ${Padding.l2} ${Padding.l4};

  display: inline-block;
`